# copy to .env for local runs. .env is gitignored and never committed.
# on GitHub these live in Settings -> Secrets and variables -> Actions

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=mnshah0307@gmail.com
SMTP_PASSWORD=              # Gmail App Password, 16 chars, NOT your login password
MAIL_TO=mnshah0307@gmail.com
